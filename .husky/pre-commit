#!/usr/bin/env sh

# Pre-commit hook для М.И.Т.А. (Husky v9)
# Запускает lint для изменённых файлов перед коммитом

echo "🔍 Running pre-commit checks..."

# Запускаем lint-staged для отлинтованных файлов
# Игнорируем ошибки ESLint (предупреждения допустимы)
npx lint-staged || true

# Примечание: type-check временно отключен до настройки тестового окружения
# npm run type-check
